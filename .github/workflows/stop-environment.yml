name: Stop Azure Environment

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment name"
        required: true
        default: "dev"
      app:
        description: "Application name"
        required: true
        default: "core"
      region:
        description: "Short region code"
        required: true
        default: "weu"

  schedule:
    - cron: "0 21 * * 1-5"

permissions:
  id-token: write
  contents: read

jobs:
  stop-environment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure login using OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          enable-AzPSSession: true

      - name: Stop Azure runtime resources
        uses: azure/powershell@v1
        with:
          azPSVersion: "latest"
          inlineScript: |
            Set-AzContext -SubscriptionId "${{ secrets.AZURE_SUBSCRIPTION_ID }}"

            ./automation/stop-environment.ps1 `
              -Environment "${{ github.event.inputs.environment || 'dev' }}" `
              -App "${{ github.event.inputs.app || 'core' }}" `
              -Region "${{ github.event.inputs.region || 'weu' }}"
